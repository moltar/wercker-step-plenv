box: wercker/default
build:
    steps:
        - validate-wercker-step
deploy:
    steps:
        - script:
            name: deploy
            code: echo "Deploy to $WERCKER_DEPLOYTARGET_NAME"
    after-steps:
        - moltar/plenv@0.1.6:
            version: 5.20.0
        - script:
            name: plenv test
            code: plenv versions; plenv global; perl -V
